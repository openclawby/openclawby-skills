# data/ — managed reference data

Misc reference files and datasets bundled with the skill. Managed centrally
(listed in `manifest.json`), refreshed on a version bump. Drop additional
reference files here and add them to `manifest.json` `managed[]`.
